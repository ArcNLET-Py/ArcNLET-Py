# .readthedocs.yml
version: 2

sphinx:
  configuration: docs/source/conf.py
  builder: html
  fail_on_warning: true

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"
  commands:
    - pip install recommonmark
